# 2026-09-13 — logging correlacionable

- Objetivo: controlar lo que sucede entre frontend y backend Laravel sin exponer información sensible.
- Alcance realizado: middleware Laravel con `X-Request-Id`, logging de inicio/fin/error, canal diario con retención configurable y logger Axios/frontend estructurado.
- Archivos afectados: `backend-laravel/app/Http/Middleware/LogHttpRequests.php`, `backend-laravel/bootstrap/app.php`, `backend-laravel/.env.example`, `backend-laravel/tests/Feature/HttpRequestLoggingTest.php`, `frontend/src/shared/logger/index.ts`, `frontend/src/shared/api/client.ts`, `docs/agent-workflow/logging.md`.
- Validaciones: Pint corrigió formato; Laravel 4/4 pruebas y 7 aserciones; frontend build correcto.
- Limitaciones: DevTools no puede escribir archivos en `frontend/logs`; la persistencia se realiza en `backend-laravel/storage/logs/laravel.log`. No se registran cuerpos, cookies, tokens, contraseñas ni query strings.
- Próximo paso: observar el mismo `request_id` en DevTools y el log Laravel durante una sesión local.
