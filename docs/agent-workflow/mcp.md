# MCP

Estado: no hay servidores MCP configurados en el repositorio y no se verificó una integración de proyecto disponible. No se añadió configuración porque el cliente soportado y el formato nativo no están confirmados aquí.

## Criterio para incorporar uno

Documentar primero la necesidad concreta, proveedor/fuente oficial, compatibilidad con el cliente, método de conexión, permisos mínimos, variables de credenciales, estado y operación inocua de verificación. Usar referencias como `${VARIABLE}`; nunca valores.

## Pendientes

Si una futura tarea necesita Drive, base de datos, observabilidad o un proveedor externo, confirmar el plugin/cliente disponible y autorización. Preferir lectura; verificar y documentar desactivación. No instalar paquetes remotos ni configurar entradas incompletas durante cambios ordinarios.
